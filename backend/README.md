# /backend

Placeholder — Spring Boot app, not yet initialized.

Initialization happens in Phase 1 (`PROJECT_TODO.md`): Spring Initializr with Web, Data JPA, Validation, Security (if using auth), PostgreSQL driver, Flyway. Package-by-feature from day one — initial packages `project/`, `contact/`; Phase 7 adds `analytics/`, `githubsync/`, `agentlog/`, `dspdemo/` as isolated packages.

See `CLAUDE.md` (repo root) for the locked-in architecture conventions this needs to follow once scaffolded.
